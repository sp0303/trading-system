# Trading System Tasks

Date: 2026-04-11
Source: derived from `CODE_AUDIT_REPORT_2026-04-11.md` plus added infra tasks for Kafka, Nginx, Docker, and Kubernetes.

## P0 - Must Do Before Monday Testing

- Fix ATR lookup inconsistency in live signal generation.
  Use `atr_14` correctly in `services/signal-service/app/main.py` instead of falling back to `0.01`.

- Fix ATR lookup inconsistency in backtesting.
  Use `atr_14` correctly in `services/signal-service/scripts/mass_backtest.py`.

- Confirm the real training-label source.
  The current parquet store appears to be missing `target_prob`, `target_mfe`, and `target_mae`.

- Make training reproducible from one authoritative pipeline.
  Either persist labels into DB/parquet or train only from a known labeled dataset.

- Rework target engineering to match the actual strategy system.
  Current labeling script is ORB-long-only and does not match the full multi-strategy design.

- Align signal filter thresholds with the intended risk policy.
  `mae_threshold` in code and docs must match.

- Mark placeholder/proxy data explicitly in APIs.
  Benchmark, institutional flow, and UI-facing confidence values should not silently look real if they are synthetic.

- Update docs to reflect actual system readiness.
  Remove or rewrite claims that Data Service and Feature Service are already fully live/runtime-ready.

## P1 - High Priority

- Replace benchmark proxy logic.
  Remove RELIANCE-as-NIFTY proxy from `services/signal-service/app/services/market_data.py`.

- Replace hardcoded breadth values with real breadth calculation.

- Add real symbol metadata.
  Sector, label, and day change should come from a maintained source, not placeholders.

- Remove placeholder confidence in `/insights`.

- Stop returning mock-success payloads from institutional service on failure.
  Return a clear degraded/error state instead.

- Add validation and safer output handling to Gemma AI service.
  Include timeout, error classification, and structured response validation.

- Review relative-strength inputs.
  Ensure `sector`, `sector_strength`, `return_percentile`, and `volume_percentile` are computed truthfully in the live data path.

## P2 - Important Stability Work

- Make signal-service DB writes transactional.
  Raw signal write and trade alert write should be consistent and rollback-safe.

- Review strategy state management for multi-worker deployment.
  Global in-memory strategy state is fragile if the service is scaled.

- Decouple sentiment service from gateway.
  It should not depend on the gateway to reach the news service.

- Remove hardcoded absolute file paths from scripts.

- Add source-quality fields in API responses.
  Example: `is_proxy`, `is_mock`, `is_stale`, `data_source`.

## P3 - Cleanup

- Clean up docs that overstate completion.

- Reduce placeholder comments and dead assumptions.

- Improve AI output rendering in frontend.
  Render markdown properly instead of line-by-line paragraph splitting.

- Expand ORM/schema coverage for `ohlcv_enriched` or switch read APIs to safer SQL/query patterns.

## Data Tasks

- Define the authoritative raw data source for live and historical paths.

- Define the authoritative labeled dataset for model training.

- Validate all parquet files for:
  timestamp consistency, timezone consistency, duplicate rows, gaps, and missing required features.

- Build a data-quality report script.
  It should detect missing columns, null ratios, invalid candles, and stale symbols.

- Add a maintained symbol master.
  Include sector, display name, exchange symbol, broker token, and benchmark mapping.

## Feature Engineering Tasks

- Build a true cross-symbol ranking pass for return and volume percentile features.

- Implement sector mapping and sector-strength computation.

- Validate all feature names for case consistency across:
  sync, parquet, model input, signal generation, and backtesting.

- Add feature-schema versioning.

## Model And Training Tasks

- Audit how the current model artifacts were trained.
  Confirm exact dataset, date range, label method, and feature schema.

- Make train/eval scripts fail fast if targets are missing.

- Add walk-forward validation outputs as required artifacts, not optional logs.

- Add model metadata artifact.
  Include train dates, feature columns, label definition, and code version.

- Add threshold tuning workflow for:
  probability, expected return, expected drawdown, anomaly filter.

- Add regression tests for prediction payload schema.

## Signal Engine Tasks

- Verify each strategy against real feature names and real data ranges.

- Add test fixtures for each strategy.
  Include one positive case and one negative case.

- Add signal deduplication and idempotency rules.

- Add runtime metrics:
  signals processed, alerts generated, model failures, DB latency.

## Frontend Tasks

- Surface real vs proxy vs stale data states visibly.

- Add explicit error states for unavailable services.

- Add per-card loading and degraded-data banners.

- Show actual benchmark source and timestamp.

- Add testing page or admin panel for service-health visibility.

## AI / Gemma Tasks

- Add model availability health check for Ollama and `gemma:2b`.

- Add structured output parsing for Gemma responses.

- Add safe fallback behavior when AI service is unavailable.

- Add prompt/version tracking for AI outputs.

- Make AI clearly advisory-only in the UI.

## Infra Tasks

- Add Dockerfiles for:
  gateway, model-service, signal-service, news-service, fundamental-service, institutional-service, sentiment-service, ai-service, frontend.

- Add a `docker-compose.yml` for local full-stack startup.
  Include app services, Postgres, and supporting infra.

- Add Nginx in front of gateway/frontend.
  Use it for reverse proxying, static frontend serving, compression, and request routing.

- Add Kafka to the task plan.
  Use Kafka for event-driven flow between ingestion, feature generation, signal processing, and downstream analytics if the system is moving beyond a simple synchronous prototype.

- Define Kafka topics.
  Example:
  `raw_market_data`, `enriched_features`, `model_requests`, `trade_signals`, `audit_events`, `service_health`.

- Define whether Kafka is truly needed now or is a phase-2 item.
  If Monday is only integration testing, Kafka should probably be planned, not introduced immediately.

- Add Kubernetes manifests or Helm charts.
  Cover deployments, services, config, secrets, ingress, and autoscaling.

- Add health probes for Kubernetes.
  Liveness and readiness for every service.

- Add centralized configuration management.
  Environment variables, secrets, model paths, DB URLs, and service URLs should not be scattered.

- Add observability stack tasks.
  Logging, metrics, tracing, and alerting.

- Add CI/CD tasks.
  Build images, run lint/checks, run smoke tests, publish artifacts, deploy to environments.

## Monday Testing Checklist

- Start all required backend services and frontend.

- Verify health endpoints.

- Verify DB connectivity.

- Verify one end-to-end `/process` call writes both raw and filtered signal records correctly.

- Verify target levels use real ATR values.

- Verify `/benchmark` is either corrected or clearly marked as proxy.

- Verify `/institutional-flow` returns truthful source state.

- Verify frontend displays degraded/proxy states where needed.

- Verify AI service graceful failure if Ollama or Gemma is unavailable.

## Suggested Execution Order

1. Fix ATR inconsistency.
2. Confirm training targets and labeling source.
3. Align risk thresholds and docs.
4. Remove or mark proxy/mock data in benchmark and institutional APIs.
5. Run Monday integration tests.
6. Add Docker and `docker-compose`.
7. Add Nginx reverse proxy.
8. Plan Kafka event flow.
9. Add Kubernetes deployment artifacts.
